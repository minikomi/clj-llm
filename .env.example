# At least one API key is required. Set the one(s) for the backend(s) you use.
OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...
OPENROUTER_KEY=sk-or-v1-...

# Optional: override the default model
# LLM_MODEL=gpt-4o-mini
